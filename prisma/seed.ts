import { PrismaClient } from "@prisma/client";
// import {}

const prisma = new PrismaClient();

const seedJobs: never[] = [];

async function main() {
  const jobPostTxs = [];

  for (const job of seedJobs) {
    // jobPostTxs.push;
    // prisma.cSEJobs.create({
    //   data: {},
    // })();
  }
}
